{
  "description": "SF Demo Scout session-startup banner: LLMGW auth, Slack MCP, active org, customer folders, update check",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session-startup.sh\""
          }
        ]
      }
    ]
  }
}
